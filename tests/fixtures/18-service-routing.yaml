apiVersion: v1
kind: Service
metadata:
  name: test-orphan-service
  namespace: gke-skills-sandbox
  labels:
    app: test-orphan-service
spec:
  selector:
    app: non-existent-backend-pod
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
